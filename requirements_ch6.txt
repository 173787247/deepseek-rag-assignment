# 第六章作业：私有化部署 DeepSeek-R1 模型的小红书文案助手
# Python 依赖包列表
# 作者：AI助手
# 日期：2025年8月

# 核心依赖
requests>=2.31.0          # HTTP 请求库，用于调用 Ollama API
typing-extensions>=4.7.1   # 类型提示扩展，支持 Python 3.7+

# 可选依赖（用于增强功能）
colorama>=0.4.6            # 跨平台彩色终端输出
rich>=13.5.0               # 富文本终端输出
tqdm>=4.65.0               # 进度条显示

# 开发依赖（可选）
pytest>=7.4.0              # 单元测试框架
black>=23.7.0              # 代码格式化工具
flake8>=6.0.0              # 代码质量检查

# 系统要求
# Python >= 3.7
# Docker Desktop
# Docker Compose
